# 🤖 Настройка Telegram Бота для приема заявок

## 📋 Что сделано:

✅ Добавлена форма обратной связи на главную страницу
✅ Создан PHP скрипт для отправки заявок в Telegram
✅ Добавлена маска для телефона (+7 (___) ___-__-__)
✅ Валидация формы
✅ Красивый UI с анимациями
✅ Интеграция с Яндекс.Метрикой (цель: form_submit)

---

## 🚀 БЫСТРАЯ НАСТРОЙКА (5 минут)

### Шаг 1: Создайте Telegram бота

1. Откройте Telegram и найдите **@BotFather**
2. Отправьте команду: `/newbot`
3. Введите имя бота: `AMG Auto Trade Bot`
4. Введите username: `amgautotrade_bot` (должен заканчиваться на _bot)
5. **СКОПИРУЙТЕ TOKEN** - это длинная строка вида:
   ```
   123456789:ABCdefGHIjklMNOpqrsTUVwxyz
   ```

### Шаг 2: Получите Chat ID

**Вариант А - Личные сообщения (рекомендуется):**

1. Напишите вашему боту в Telegram любое сообщение (например: "Привет")
2. Откройте в браузере:
   ```
   https://api.telegram.org/bot<ВАШ_TOKEN>/getUpdates
   ```
   (замените <ВАШ_TOKEN> на токен из Шага 1)

3. Найдите в ответе:
   ```json
   "chat": {
       "id": 123456789,
       ...
   }
   ```
   Это число (123456789) - ваш **Chat ID**

**Вариант Б - Группа/Канал:**

1. Создайте группу или канал в Telegram
2. Добавьте бота в группу (как администратора для каналов)
3. Напишите в группе любое сообщение
4. Откройте в браузере тот же URL из п.2
5. Chat ID будет начинаться с минуса: `-123456789`

### Шаг 3: Настройте telegram-bot.php

1. Откройте файл `telegram-bot.php`
2. Найдите строки 30-34:
   ```php
   define('TELEGRAM_BOT_TOKEN', 'ВАXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX');
   define('TELEGRAM_CHAT_ID', 'ВАXXXXXXXXX');
   ```

3. Замените на ваши значения:
   ```php
   define('TELEGRAM_BOT_TOKEN', '123456789:ABCdefGHIjklMNOpqrsTUVwxyz');
   define('TELEGRAM_CHAT_ID', '123456789');
   ```

4. Сохраните файл

### Шаг 4: Проверьте работу

1. Откройте в браузере:
   ```
   https://amgautotrade.ru/telegram-bot.php?test=1
   ```

2. Вы увидите страницу с проверкой:
   - ✓ Bot Token настроен
   - ✓ Chat ID настроен
   - ✓ Подключение к Telegram API успешно
   - ✓ Тестовое сообщение отправлено

3. **Проверьте Telegram** - должно прийти тестовое сообщение!

---

## 📱 Формат сообщений в Telegram

Когда клиент отправляет заявку, вы получите сообщение:

```
🚗 НОВАЯ ЗАЯВКА С САЙТА

👤 Имя: Иван Петров
📞 Телефон: +7 (915) 054-89-83
💬 Комментарий: Интересует BMW X5

📍 Источник: Главная страница
🕐 Дата: 24.12.2025 15:30:45

Перезвоните клиенту в течение 5 минут!
```

---

## 🔧 Расширенные настройки

### Отправка в несколько чатов

Если нужно отправлять в несколько групп/каналов:

```php
define('TELEGRAM_CHAT_IDS', ['123456789', '-987654321']);

// В функции sendToTelegram добавьте цикл:
foreach (TELEGRAM_CHAT_IDS as $chatId) {
    // отправка...
}
```

### Логирование

Все запросы логируются в файл `telegram-bot.log`:
```php
define('ENABLE_LOGGING', true);  // включить/выключить
```

### Дополнительные поля

Можно добавить любые поля в форму и они попадут в сообщение.

Пример: добавьте email в форму (index.html):
```html
<input type="email" id="userEmail" name="email" placeholder="Email">
```

И в telegram-bot.php:
```php
$email = isset($data['email']) ? $data['email'] : '';
if (!empty($email)) {
    $telegramMessage .= "📧 <b>Email:</b> {$email}\n";
}
```

---

## 🎨 Кастомизация формы

### Изменить текст заголовка

Файл: `index.html`, строки 1724-1725:
```html
<h2 class="callback-title">Оставьте заявку</h2>
<p class="callback-subtitle">Перезвоним за 5 минут и ответим на все вопросы</p>
```

### Изменить цвет кнопки

Файл: `index.html`, найдите `.form-submit` в стилях (строка ~1487):
```css
.form-submit {
    background: var(--gold); /* Замените на #ВАШ_ЦВЕТ */
}
```

---

## 📊 Интеграция с Яндекс.Метрикой

Уже настроено! При отправке формы срабатывает цель `form_submit`.

Настройте цель в Яндекс.Метрике:
1. Зайдите в счетчик 105327533
2. Настройки → Цели → Добавить цель
3. JavaScript событие: `form_submit`

---

## 🐛 Решение проблем

### "Ошибка отправки" при заполнении формы

**Причина:** PHP скрипт не может отправить запрос в Telegram API.

**Решение:**
1. Проверьте, что на хостинге установлен PHP с поддержкой cURL
2. Проверьте, что разрешены исходящие HTTPS запросы
3. Откройте `telegram-bot.log` и посмотрите ошибку

### Бот не получает сообщения

**Причина:** Неправильный Chat ID или бот не добавлен в группу.

**Решение:**
1. Проверьте, что вы написали боту или добавили его в группу
2. Убедитесь, что Chat ID скопирован правильно (без лишних символов)
3. Для групп Chat ID должен начинаться с минуса: `-123456789`

### "Bot was blocked by the user"

**Причина:** Вы заблокировали бота в Telegram.

**Решение:**
1. Найдите бота в Telegram
2. Нажмите "РАЗБЛОКИРОВАТЬ" или "START"
3. Попробуйте снова

---

## 📧 Дополнительно: Email уведомления

Если хотите дублировать заявки на email, раскомментируйте в `telegram-bot.php` (строка 143):

```php
sendEmail($name, $phone, $message);
```

И добавьте функцию:
```php
function sendEmail($name, $phone, $message) {
    $to = 'info@amgautotrade.ru';
    $subject = 'Новая заявка с сайта';
    $body = "Имя: {$name}\nТелефон: {$phone}\nСообщение: {$message}";
    $headers = 'From: noreply@amgautotrade.ru' . "\r\n";

    mail($to, $subject, $body, $headers);
}
```

---

## ✅ Checklist после настройки

- [ ] Telegram бот создан через @BotFather
- [ ] TOKEN и CHAT_ID вставлены в telegram-bot.php
- [ ] Тестовая страница показывает успешное подключение
- [ ] Тестовое сообщение получено в Telegram
- [ ] Форма на сайте отправляет заявки
- [ ] Настроена цель в Яндекс.Метрике
- [ ] Файл telegram-bot.php загружен на сервер
- [ ] На хостинге есть PHP с cURL

---

## 🎯 Что дальше?

После настройки бота рекомендую:

1. **Добавить форму в каталог** - чтобы клиенты могли спрашивать о конкретных автомобилях
2. **Добавить липкую кнопку на мобильных** - "Заказать звонок" всегда на экране
3. **Добавить видимый телефон в хедер** - чтобы клиенты сразу видели номер
4. **Настроить автоответчик** - бот может сразу отвечать "Заявка принята, менеджер свяжется..."

Нужна помощь с этим? Дайте знать!

---

**Создано:** 24.12.2025
**Версия:** 1.0
**Поддержка:** Если возникли вопросы - пишите!
